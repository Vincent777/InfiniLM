name: CI

on:
  push:
    branches: ["ci_test"]
  pull_request:
    branches: ["ci_test"]

jobs:
  ci:
    uses: InfiniTensor/ci/.github/workflows/infiniops-ci.yml@infiniCore_ci
    with:
      config_path: .github/ci_config.yaml
      ci_ref: infiniCore_ci
    secrets: inherit
