name: CI

on:
  push:
    branches: ["refactor/inline-infinicore"]
  pull_request:
    branches: ["refactor/inline-infinicore"]

jobs:
  ci:
    uses: InfiniTensor/ci/.github/workflows/infinilm-ci.yml@infiniCore_ci
    with:
      config_path: .github/ci_config.yaml
      ci_ref: infiniCore_ci
    secrets: inherit
